/**
 * tceb-dashboard-builder — library entry point
 *
 * การใช้งานใน host app:
 *   import DashboardBuilder from 'tceb-dashboard-builder';
 *   <DashboardBuilder apiBaseUrl="https://..." />
 */
export { default } from './DashboardBuilderComponent.jsx';
